import {
    addCopyToCodeBlocks,
    ANIMATION_DURATION_DEFAULT,
    animation_duration,
    animation_easing,
    callPopup,
    is_send_press,
    MAX_INJECTION_DEPTH,
    reloadMarkdownProcessor,
    scrollChatToBottom,
    setAnimationDuration,
} from '../ui-core.js';

export {
    addCopyToCodeBlocks,
    ANIMATION_DURATION_DEFAULT,
    animation_duration,
    animation_easing,
    callPopup,
    is_send_press,
    MAX_INJECTION_DEPTH,
    reloadMarkdownProcessor,
    scrollChatToBottom,
    setAnimationDuration,
};
