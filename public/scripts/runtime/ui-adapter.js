import {
    ANIMATION_DURATION_DEFAULT,
    animation_duration,
    callPopup,
    is_send_press,
    MAX_INJECTION_DEPTH,
    reloadMarkdownProcessor,
    scrollChatToBottom,
    setAnimationDuration,
} from '../../script.js';

export {
    ANIMATION_DURATION_DEFAULT,
    animation_duration,
    callPopup,
    is_send_press,
    MAX_INJECTION_DEPTH,
    reloadMarkdownProcessor,
    scrollChatToBottom,
    setAnimationDuration,
};
